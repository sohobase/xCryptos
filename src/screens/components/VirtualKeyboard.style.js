import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '50%',
    minHeight: '50%',
    flex: 0,
    flexDirection: 'row',
    flexWrap: 'wrap',
    backgroundColor: 'white',
  },
});
