import { StyleSheet } from 'react-native';
import { THEME } from '../../config';

const { WHITE } = THEME;

export default StyleSheet.create({
  icon: {
    tintColor: WHITE,
  },

  list: {
    flex: 1,
    width: '100%',
  },
});
