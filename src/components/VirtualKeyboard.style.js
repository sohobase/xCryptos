import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '45%',
    minHeight: '45%',
    flex: 0,
    flexDirection: 'row',
    flexWrap: 'wrap',
    backgroundColor: 'white',
  },
});
