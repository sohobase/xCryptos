import { StyleSheet } from 'react-native';
import { THEME } from '../../config';

export default StyleSheet.create({
  container: {
    // margin: THEME.UNIT * 0.1,
  },

  loading: {
    alignItems: 'center',
    alignContent: 'center',
    justifyContent: 'center',
  },
});
