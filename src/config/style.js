import { StyleSheet } from 'react-native';
import THEME from './theme';

export default StyleSheet.create({
  SCREEN: {
    flex: 1,
  },
  BUTTON: {
    color: THEME.WHITE,
    backgroundColor: 'red',
  },
  DRAWER_ICON: {

  },
});
